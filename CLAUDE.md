# CLAUDE.md

The instructions for this repository live in [AGENTS.md](AGENTS.md).

Read `AGENTS.md` before doing anything else in this repository, and follow it.
This file is a pointer only — it is intentionally a regular file rather than a
symlink, because symlinks do not work reliably on Windows checkouts. Nothing
should be added here; put repository instructions in `AGENTS.md` so that every
agent, regardless of which filename it looks for, reads one source of truth.
